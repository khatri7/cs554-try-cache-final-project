{
	"name": "server",
	"version": "1.0.0",
	"description": "",
	"type": "module",
	"main": "server.js",
	"eslintConfig": {
		"env": {
			"es2021": true,
			"node": true
		},
		"extends": [
			"airbnb-base",
			"plugin:prettier/recommended"
		],
		"plugins": [
			"prettier"
		]
	},
	"scripts": {
		"start": "node --experimental-specifier-resolution=node server.js",
		"server": "node --watch --experimental-specifier-resolution=node server.js",
		"test": "echo \"Error: no test specified\" && exit 1"
	},
	"author": "",
	"license": "MIT",
	"dependencies": {
		"express": "^4.18.2"
	}
}
